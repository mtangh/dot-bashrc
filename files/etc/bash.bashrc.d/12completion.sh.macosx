# ${bashrc_dir}/12completion.sh.macosx
# $Id$

# completion dierctory
for complet_sh in $( {
__pf_rc_loader \
"${HOME}"/{.macOS,.MacOSX}/{etc/,}bash_completion.d/*.sh
do
  [ -f "${complet_sh}" ] &&
  [ -x "${complet_sh}" ] &&
  . "${complet_sh}" || :
done 2>/dev/null || :
unset complet_sh

# *eof*
